# WebDisplay 2.4.0 WinUI 3 验证记录

验证日期：2026-09-10。验证环境：Windows 11 x64，.NET SDK 10.0.401，Microsoft.WindowsAppSDK.WinUI 2.3.6、InteractiveExperiences 2.1.6，Microsoft.Windows.SDK.BuildTools 10.0.26100.9169，传递依赖 Microsoft.Web.WebView2 1.0.3719.77，已安装 WebView2 Runtime 152，Inno Setup 6.7.3。

## 已完成

- WinUI 3 Release 编译通过。主窗口和设置窗口均为 Microsoft.UI.Xaml.Window；不再依赖 WPF 或 Windows Forms。
- 已发布包含 .NET 与 WinUI 3 运行依赖的多文件目录，并生成中英文 Inno Setup 安装包、发布 ZIP 和源码 ZIP。发布程序通过下列 20 项自检及 37 项真实运行检查，退出码均为 0；安装后的程序再次通过 20 项自检。
- 从 Windows App SDK 总包改为直接引用程序需要的组件后，发布目录中不再包含 ONNX、DirectML 等未使用的 AI 运行库；主程序 EXE 为 300,032 字节。XAML 资源编译并保留在 PRI 中，设置窗口和主窗口可正常加载。
- 20 项安全自检：网址校验、刷新间隔、配置保存和损坏恢复、重试间隔、每天/每周重启规则、主题选项、缩放范围、旧配置默认 100% 和自定义比例持久化，包含旧配置默认显示滚动条及隐藏选项序列化/磁盘持久化验证，新增证书忽略默认关闭、旧配置保持校验及开关序列化/磁盘持久化验证。
- 37 项真实 WinUI 3 / WebView2 运行检查通过：原有网页、刷新、恢复、窗口、电源和主题检查，加上 9 项缩放检查、8 项滚动条检查及 7 项证书开关检查。
- HTTPS 检查覆盖默认拒绝自签名证书、通过设置窗口保存后允许加载、再次刷新和浏览器进程崩溃后继续生效、关闭后退出旧浏览器会话并拒绝此前已放行的同一证书、无浏览器控件时保存仍能恢复加载、持久 cookies 保留，以及取消修改保持证书校验。未使用浏览器全局忽略证书参数，未修改其他应用的证书策略。
- 在真实 HTML 页面测量固定宽度元素的渲染尺寸，验证 25%、50%、75%、150%、200%、500% 和 100%；多次调整不会累乘。验证恢复 100% 时还原网站原有的内联 zoom 和 important 优先级、iframe 不重复应用缩放、非法输入被拒绝、“恢复100%”仅改变未保存输入、自定义 137% 保存和读取、自动刷新/网络恢复/浏览器重建后继续使用 137%，以及取消修改保持页面尺寸及已保存设置。
- 滚动条检查覆盖：主网页和滚动容器的原生横纵滚动条隐藏，原生鼠标滚轮输入及程序滚动仍正常，重新开启恢复网页样式，保存后应用、取消不更改，以及自动刷新、网络恢复、浏览器重建后继续隐藏。测试元素设置明确的原生滚动条宽度，以免系统覆盖式滚动条偏好影响尺寸断言。
- 本版设置窗口深浅色两页及 HTTPS 证书卡片均生成 PNG 并检查，忽略 SSL/TLS 证书错误开关、保存后重载提示及风险说明完整可见；底部保存和取消按钮固定可见。此前 WinUI 3 迁移验收已检查深色原生标题栏、设置页切换与滚动、账号设置区域和正常退出。
- 此前 WinUI 3 迁移验收已在网页获得焦点时验证 F11 全屏、全屏状态下 Ctrl+, 打开设置、Esc 退出全屏，本次保留相同实现。快捷键仅在本应用窗口前台生效，使用当前 UI 线程消息处理，没有注册全局热键。
- 浏览器环境和控件初始化分别有 30 秒、45 秒超时，失败后进入自动重试，避免初始化阶段无限等待。
- 迁移沿用原系统服务代码。该代码已通过 11 项系统服务检查：命名管道 ACL、客户端/服务端进程 ID 校验、非法操作拒绝、任务时间和参数，以及任务计划程序对每日和每周 XML 的 TASK_VALIDATE_ONLY 验证（没有注册任务）。

HTTPS 测试使用本机回环地址、随机端口和临时自签名证书。证书和私钥在内存生成并通过匿名管道交给 Node 内置 HTTPS 服务，不创建私钥文件、不导入信任库；没有使用用户证书或私钥。Node 仅用于显式运行开发冒烟测试，不随安装包分发，也不是正常运行依赖。网络恢复测试只停止并恢复本机回环地址上的测试 HTTP 服务，没有断开用户电脑的网络连接。浏览器崩溃测试只终止本测试实例通过 WebView2 API 返回的浏览器进程。自动截图会等待 WinUI Pivot 切页动画结束。

## 安装包验证

- 已在工作区内含中文、空格的隔离路径 `artifacts\certificates-installer-test\应用 安装` 安装上一版 2.3.0，再使用新版安装包升级至 2.4.0。安装及升级退出码均为 0；安装后全部发布文件的 SHA-256 与发布目录一致，当前用户卸载注册表项显示版本 2.4.0。
- 已存在 WebView2 Runtime 时，安装日志确认跳过运行组件安装；没有触发电脑重启。缺少 Runtime 时使用随包附带、已校验微软有效数字签名的官方引导安装器，此电脑未卸载共享 Runtime 来测试缺失场景。
- 沿用已在 2.1.0 检查的中文安装向导，文字、页面和桌面快捷方式选项正常；向导配置为跟随系统深浅色。该次向导视觉检查使用系统浅色模式。
- 升级后静默卸载，退出码为 0。隔离数据目录中的设置文件在升级和卸载前后 SHA-256 一致；安装的主程序及其当前用户卸载注册表项已移除。
- 卸载脚本仅在当前用户启动项精确指向本安装位置时删除该项；没有删除任务计划、修改 Winlogon 或 LSA 的逻辑。此项通过代码检查验证，未在开发电脑上创建真实开机启动项。
- 安装包中的文件保持正常多文件布局，不在每次启动时释放单文件运行依赖；源码包包含安装脚本、构建脚本、中文语言文件及 WebView2 引导安装器。

## 部署时还需验证

未修改开发电脑的开机启动、自动登录或重启任务，也没有重启电脑。实际 UAC 授权/取消、跨管理员账号授权、自动登录与实际计划重启，需在目标电脑上明确启用后验证。域策略、网站登录、长期连续运行、多显示器混合 DPI、系统配色在程序运行中切换以及缺失 Runtime 的安装流程尚未做完整部署验收。

缩放使用网页根元素的 CSS zoom，100% 恢复网页自身样式。此机制会重排内容，但不等同于浏览器原生缩放；媒体查询、视口单位、canvas、特殊固定布局及 PDF 查看器需按目标网站验收。

滚动条开关通过 WebView2 的 `Emulation.setScrollbarsHidden` 控制原生绘制，不修改网页 overflow 样式。命令串行执行，更改该选项并保存时刷新展示网页；不支持时显示提示而不进入浏览器重建循环。独立进程中的跨站 iframe 及网页自绘滚动控件可能仍显示滚动条；开启开关恢复网页原有策略，不强制添加网站原本隐藏或不需要的滚动条。

证书开关通过 WebView2 的 `ServerCertificateErrorDetected` 处理当前应用网页和资源的服务器证书错误。更改并保存时清除放行决定、关闭控件并等待旧浏览器会话退出，然后使用原数据目录重新创建。清理或退出等待失败时保持旧控件关闭，并进入重试；不会回退到放行。运行检查使用自签名证书，不代表对过期、撤销、名称不匹配、企业代理、跨站资源和各种服务器 TLS 配置均已逐一实测；协议、加密协商和 mTLS 客户端身份验证失败不保证能够通过此选项解决。

## 重现

```powershell
.\scripts\build.ps1
.\dist\WebDisplay-WinUI3-win-x64\WebDisplay.exe --self-test --data-dir .\artifacts\manual-self-test
.\dist\WebDisplay-WinUI3-win-x64\WebDisplay.exe --smoke-test --data-dir .\artifacts\manual-smoke-test
```

`--self-test` 输出 `self-test-result.json`。包含 HTTPS 检查的 `--smoke-test` 需要本机 Node.js；可设置 `WEBDISPLAY_TEST_NODE` 为 node.exe 的绝对路径，未设置时从 PATH 查找。`--smoke-test` 输出 `smoke-test-result.json` 和窗口预览 PNG，过程会临时显示窗口、开启后释放防休眠请求，并终止/重建本测试实例的浏览器进程。应在正常交互式 Windows 会话运行，不能用受限进程沙箱的 WebView2 故障来判定正常桌面下的运行结果。

本版开发会话的自检结果和截图保存在 `artifacts/certificates-validation`，安装日志与交付文件验证保存在 `artifacts/certificates-installer-test`；2.2.0 的验证在 `artifacts/zoom-validation` 和 `artifacts/zoom-installer-test`，系统服务测试工具源码在 `artifacts/servicecheck`，均不随用户发布包分发。自动验证不等同于长时间运行或目标企业域验收。


