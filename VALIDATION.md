# WebDisplay 2.1.0 WinUI 3 验证记录

验证日期：2026-09-09。验证环境：Windows 11 x64，.NET SDK 10.0.401，Microsoft.WindowsAppSDK.WinUI 2.3.6、InteractiveExperiences 2.1.6，Microsoft.Windows.SDK.BuildTools 10.0.26100.9169，传递依赖 Microsoft.Web.WebView2 1.0.3719.77，已安装 WebView2 Runtime 152，Inno Setup 6.7.3。

## 已完成

- WinUI 3 Release 编译通过，无警告、无错误。主窗口和设置窗口均为 Microsoft.UI.Xaml.Window；不再依赖 WPF 或 Windows Forms。
- 已发布包含 .NET 与 WinUI 3 运行依赖的多文件目录，并生成中英文 Inno Setup 安装包、发布 ZIP 和源码 ZIP。安装版在包含中文和空格的测试路径中，通过下列 16 项自检及 13 项真实运行检查，退出码均为 0。
- 从 Windows App SDK 总包改为直接引用程序需要的组件后，发布目录中不再包含 ONNX、DirectML 等未使用的 AI 运行库；主程序 EXE 为 300,032 字节。XAML 资源编译并保留在 PRI 中，设置窗口和主窗口可正常加载。
- 16 项安全自检：网址校验、刷新间隔、配置保存和损坏恢复、重试间隔、每天/每周重启规则，以及新增主题选项、旧配置兼容和主题持久化。
- 13 项真实 WinUI 3 / WebView2 运行检查通过：HTTP 网页和 JavaScript、定时刷新及关闭刷新、本地网站不可达后自动恢复、结束本测试实例的浏览器进程后重建、全屏还原、置顶窗口样式、电源请求开启/释放、最小化释放/恢复展示重新申请电源请求、深色/浅色窗口和网页配色同步、跟随系统选项、取消设置恢复原主题。
- 本版安装后的设置窗口深浅色两页均生成 PNG；此前 WinUI 3 迁移验收已检查底部操作按钮、深色原生标题栏、设置页切换与滚动、账号设置区域、取消返回展示以及正常退出，相关界面代码未改变。
- 此前 WinUI 3 迁移验收已在网页获得焦点时验证 F11 全屏、全屏状态下 Ctrl+, 打开设置、Esc 退出全屏，本次保留相同实现。快捷键仅在本应用窗口前台生效，使用当前 UI 线程消息处理，没有注册全局热键。
- 浏览器环境和控件初始化分别有 30 秒、45 秒超时，失败后进入自动重试，避免初始化阶段无限等待。
- 迁移沿用原系统服务代码。该代码已通过 11 项系统服务检查：命名管道 ACL、客户端/服务端进程 ID 校验、非法操作拒绝、任务时间和参数，以及任务计划程序对每日和每周 XML 的 TASK_VALIDATE_ONLY 验证（没有注册任务）。

网络恢复测试只停止并恢复本机回环地址上的测试 HTTP 服务，没有断开用户电脑的网络连接。浏览器崩溃测试只终止本测试实例通过 WebView2 API 返回的浏览器进程。自动截图会等待 WinUI Pivot 切页动画结束。

## 安装包验证

- 已从实际生成的安装包安装到工作区内含中文、空格的隔离路径 `artifacts\installer-test\应用 安装`，安装退出码为 0。安装后的主程序 SHA-256 与发布目录中的文件一致，当前用户卸载注册表项显示版本 2.1.0。
- 已存在 WebView2 Runtime 时，安装日志确认跳过运行组件安装；没有触发电脑重启。缺少 Runtime 时使用随包附带、已校验微软有效数字签名的官方引导安装器，此电脑未卸载共享 Runtime 来测试缺失场景。
- 已运行中文安装向导预览，文字、页面和桌面快捷方式选项正常；向导配置为跟随系统深浅色。本次向导视觉检查使用系统浅色模式。
- 已在相同测试目录覆盖安装一次，随后静默卸载，退出码均为 0。隔离数据目录中的设置文件在升级和卸载前后 SHA-256 一致；安装的主程序及其当前用户卸载注册表项已移除。
- 卸载脚本仅在当前用户启动项精确指向本安装位置时删除该项；没有删除任务计划、修改 Winlogon 或 LSA 的逻辑。此项通过代码检查验证，未在开发电脑上创建真实开机启动项。
- 安装包中的文件保持正常多文件布局，不在每次启动时释放单文件运行依赖；源码包包含安装脚本、构建脚本、中文语言文件及 WebView2 引导安装器。

## 部署时还需验证

未修改开发电脑的开机启动、自动登录或重启任务，也没有重启电脑。实际 UAC 授权/取消、跨管理员账号授权、自动登录与实际计划重启，需在目标电脑上明确启用后验证。域策略、网站登录、长期连续运行、多显示器混合 DPI、系统配色在程序运行中切换以及缺失 Runtime 的安装流程尚未做完整部署验收。

## 重现

```powershell
.\scripts\build.ps1
.\dist\WebDisplay-WinUI3-win-x64\WebDisplay.exe --self-test --data-dir .\artifacts\manual-self-test
.\dist\WebDisplay-WinUI3-win-x64\WebDisplay.exe --smoke-test --data-dir .\artifacts\manual-smoke-test
```

`--self-test` 输出 `self-test-result.json`。`--smoke-test` 输出 `smoke-test-result.json` 和窗口预览 PNG，过程会临时显示窗口、开启后释放防休眠请求，并终止/重建本测试实例的浏览器进程。应在正常交互式 Windows 会话运行，不能用受限进程沙箱的 WebView2 故障来判定正常桌面下的运行结果。

开发会话的安装日志、自检结果和截图保存在 `artifacts/installer-test`；系统服务测试工具源码保存在 `artifacts/servicecheck`，均不随用户发布包分发。自动验证不等同于长时间运行或目标企业域验收。
